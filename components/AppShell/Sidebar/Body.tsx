import { Container, Text } from "@mantine/core";

const Body = () => {
  return (
    <Container sx={{ flexGrow: "1" }}>
      <Text size="xl">[List of Users]</Text>
    </Container>
  );
};

export default Body;
